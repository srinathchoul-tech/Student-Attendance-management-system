# Firebase Backend Setup

This guide explains how to set up and deploy your backend for the **IPA System** project. It includes Firestore, Cloud Functions, and seeding demo data.

---

## ⚙️ 1️⃣ Install Firebase Admin SDK
Install the Firebase Admin SDK in Python:

```bash
python -m pip install firebase-admin
